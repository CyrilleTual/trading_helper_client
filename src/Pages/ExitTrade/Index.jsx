import { useParams } from "react-router-dom";

function ExitTrade() {
  const { tardeId } = useParams();

  // va recupérer les infos du trade 


  return (
    <div>ExitTrade</div>
  )
}

export default ExitTrade